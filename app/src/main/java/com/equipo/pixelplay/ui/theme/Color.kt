package com.equipo.pixelplay.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Paleta PixelPlay — gamer sobrio: violeta neón sobre fondos casi negros.
 */

// Acentos neón
val NeonViolet = Color(0xFF9D4EDD)   // primary (oscuro)
val NeonMagenta = Color(0xFFE05FC4)  // tertiary
val CyberCyan = Color(0xFF4EE0D0)    // secondary acento

// Variantes para tema claro (más profundas para contraste)
val DeepViolet = Color(0xFF6A2CB5)   // primary (claro)
val DeepMagenta = Color(0xFFA83E8C)
val DeepCyan = Color(0xFF1E8F82)

// Superficies oscuras
val AlmostBlack = Color(0xFF0F0D17)  // background (oscuro)
val SurfaceDark = Color(0xFF1A1726)  // surface (oscuro)
val OnDark = Color(0xFFECE7F5)       // texto sobre oscuro

// Superficies claras
val AlmostWhite = Color(0xFFF7F5FB)  // background (claro)
val SurfaceLight = Color(0xFFFFFFFF) // surface (claro)
val OnLight = Color(0xFF1B1726)      // texto sobre claro
